"use strict"

import $ from 'jquery';

export default class TextMessageHandler{
	constructor(){

	}

	sendText(){
	let to = $("#sendTextPhoneNumber")[0].value;
	let msg = $("#sendTextComment")[0].value;
	if(msg && to){
	
	}
}
}